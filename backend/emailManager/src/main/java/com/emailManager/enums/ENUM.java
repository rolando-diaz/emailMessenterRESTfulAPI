package com.emailManager.enums;


public interface ENUM {
    /**
     * This interface is implemented by both the FAILED enum class and the SUCCESS enum class.
     * Hence, any method 'method(ENUM enum)' is allowed to receive Objects from both classes
     */
}
